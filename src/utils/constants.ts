enum RolesType {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

enum PrivacySetting {
  PUBLIC = 'PUBLIC',
  FRIENDS_ONLY = 'FRIENDS_ONLY',
  PRIVATE = 'PRIVATE',
}

const ROLES_KEY = 'roles';

export { RolesType, PrivacySetting, ROLES_KEY };
